#include "mySort.h"

void mySort(int array[], unsigned int first, unsigned int last) {

int i, a, j;
for(i = 0; i < last; i++){

  a = array[i];
  j = i-1;
     while(j >= 0 && array[j] > a){
	array[j+1] = array[j];
	j = j-1;
}

 array[j+1] = a;

 
 }

}
